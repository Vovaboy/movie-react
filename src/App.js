import React from 'react';
import {MoviesList} from "./components/MoviesList/MovieList";

const App = () => {
    return (
        <div>
            <MoviesList/>
        </div>
    );
};

export default App;